<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.lyh.problem">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".EnshrineActivity" />
        <activity android:name=".ManuscriptActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".UserInfoActivity" />
        <activity android:name=".ProblemActivity" />
        <activity android:name=".AddReplyActivity" />
        <activity android:name=".EditUserInfoActivity" />
        <activity android:name=".TopicActivity" />
        <activity android:name=".ProblemReplyActivity" />
        <activity android:name=".MoreActivity" />
        <activity android:name=".QuizedActivity" />
        <activity android:name=".RepliedActivity" />
    </application>

</manifest>
